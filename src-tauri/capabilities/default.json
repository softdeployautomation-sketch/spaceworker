{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the SpaceWorker OS desktop shell.",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:allow-write-text-file"
  ]
}