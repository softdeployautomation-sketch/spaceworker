# Replace %INTERNAL_BEARER_TOKEN% with the actual value from .env before installing.
# Install: sudo cp payment-verify.service /etc/systemd/system/
[Unit]
Description=SpaceWorker payment verification poller

[Service]
Type=oneshot
ExecStart=/usr/bin/curl -s -X POST \
  -H "Authorization: Bearer %INTERNAL_BEARER_TOKEN%" \
  http://localhost:3000/api/internal/payment-verify